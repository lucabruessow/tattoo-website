---
---
<button>Book on Instagram</button>

<style>
    button {
        background-color: var(--color-frosted);
        backdrop-filter: blur(6px);
        color: var(--c-neutral-50);
        border: 1px var(--color-border);
        border-radius: var(--radius-lg);
        padding: var(--space-xs) var(--space-md);
        cursor: pointer;
    }
</style>

